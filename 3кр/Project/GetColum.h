#pragma once
#include <iostream>
#include <vector>

vector<int> GetColum(vector<vector<int>> matrix, int colum, int lines);
void SetColum(vector<vector<int>>& matrix, vector<int> colum, int column_number, int lines);